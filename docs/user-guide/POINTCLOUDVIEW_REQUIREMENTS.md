# pointcloudview 点云处理程序

主程序：`pointcloudview.exe`

界面：`mainwindow.ui`

核心算法：`pointcloudprocessor.cpp/.h`

当前技术栈：Qt 6.8.3 + C++17 + QOpenGLWidget + OpenGL 3.3 Core。

本文档只记录当前有效功能、默认算法、问题处理、输出、验证和参数建议。机器人世界
坐标拼接、Python、CUDA、PCL、VTK 和 Open3D 不属于主程序当前功能。

## 已支持的数据和功能

- 支持 ASCII PLY、Binary Little Endian PLY 和 Binary Big Endian PLY。
- 读取顶点 `x/y/z` 和可选法向量 `nx/ny/nz`，属性按名称匹配。
- 支持打开单个 PLY，或递归扫描文件夹中的全部 `.ply`。
- 文件夹扫描结果全部显示在左侧数据源列表，点击后逐文件预览。
- 支持点云旋转、平移、滚轮缩放和重置视角。
- 支持真实点体素预处理和统计离群值去除。
- 支持 GPU Picking 精确选择三个真实点并提取 2.5D 平面。
- 支持平面边缘分割、黄色边缘选择和纯黑背景二维图片输出。

### PLY 第一阶段加载优化

- 主查看器后台加载统一调用共享 `pcv::detail::io::readPly`，避免应用层维护第二套解析器。
- 按 PLY header 声明的顶点数一次性分配并按索引写入，减少 `QVector` 动态扩容。
- 解析过程中同步计算有限 XYZ 包围盒，主线程不再为设置 Z 映射范围重复遍历全量点。
- 保持 ASCII、binary little-endian、binary big-endian、点顺序、法向量和错误检查不变。

### PLY ASCII 解析优化

- ASCII 坐标和法向量使用无区域依赖的快速浮点解析器，支持正负号、小数和科学计数法。
- 根据 Header 计算最后一个必需属性位置，后续颜色、强度等无关属性不再转换，只跳过到行尾。
- 错误数字、非有限值和溢出值仍会被拒绝；二进制读取路径不变。

当前画布缓存 `m_points` 是全部后续处理的唯一输入。加载或处理得到新点云后，
必须先发布到画布，再清除旧平面、边缘和选点状态。

## 启动

在 Qt Creator 中打开：

```text
D:/workpiece/pointcloudview/pointcloudview/CMakeLists.txt
```

选择 `Desktop Qt 6.8.3 MSVC2022 64bit`，不要使用 MinGW。Windows 源码和构建
路径只能包含 ASCII 字符。

命令行构建：

```powershell
cmake -S D:/workpiece/pointcloudview/pointcloudview `
      -B D:/workpiece/pointcloudview/pointcloudview/build-clean `
      -DCMAKE_PREFIX_PATH=C:/Qt/6.8.3/msvc2022_64 `
      -DPCV_BUILD_TESTS=ON

cmake --build D:/workpiece/pointcloudview/pointcloudview/build-clean `
      --config Debug --parallel 2

ctest --test-dir D:/workpiece/pointcloudview/pointcloudview/build-clean `
      -C Debug --output-on-failure
```

启动失败时检查：

```text
%LOCALAPPDATA%/PointCloudSuite/pointcloudview/logs/startup.log
```

程序启动时必须保留：

```cpp
qputenv("QT_OPENGL", QByteArrayLiteral("desktop"));
QApplication::setAttribute(Qt::AA_UseDesktopOpenGL);
```

同时保留 NVIDIA/AMD 高性能 GPU 导出标记。实际显卡只按 `GL_VENDOR`、
`GL_RENDERER` 和 `GL_VERSION` 判断，不根据 GPU 编号推断。

## 默认处理流程

1. 后台读取 PLY，校验文件头、属性类型、顶点数量和数据完整性。
2. 优先读取用户本地应用数据目录中有效的 `.pcvbin`；首次解析后建立二进制缓存。
3. 将完整真实点发布到当前画布缓存并上传 OpenGL VBO。
4. 可选执行体素预处理和统计离群值去除，处理结果替换当前缓存。
5. 使用 Picking FBO 在画布选择 P1、P2、P3。
6. 三点生成初始平面，使用 RANSAC 排除离群点，再用 PCA/最小二乘精拟合。
7. 对最终平面做距离分类和连通域筛选，平面内点显示灰色。
8. 对确认平面建立 UV 栅格、二值 Mask、形态学处理和连续边界。
9. 在画布显示黄色边缘，并可输出纯黑背景二维图片。

全部耗时算法通过 `QtConcurrent` 在后台运行。工作线程不得访问 QWidget、调用
`update()`、`makeCurrent()` 或直接操作 OpenGL。异步结果必须校验
`m_canvasRevision`，过期结果直接丢弃。

## PLY 加载和缓存

### 加载规则

- 顶点属性按名称读取，不依赖固定列顺序。
- 缺少 `x/y/z`、文件头不完整、类型不支持或数据截断时拒绝加载并说明原因。
- 文件夹扫描只建立数据源列表，不自动合并或配准。
- 空点云不得发布到画布。

### 缓存位置

运行缓存、启动日志和导出文件按用途保存到用户本地应用数据目录：

```text
%LOCALAPPDATA%/PointCloudSuite/pointcloudview/
├─ cache/
├─ logs/
└─ exports/
```

单文件缓存：

```text
cache/ply_<清理后的文件名>_<绝对路径哈希>.pcvbin
```

缓存头校验版本、点数、源文件大小和修改时间。运行数据不写入源码树。

### 加载速度优化

当前主要耗时来自 ASCII 浮点解析、首次缓存写入、包围盒遍历和完整 VBO 上传。
后续优化顺序：

- 分别记录文件头、解析、缓存、包围盒、VBO 和首帧耗时。
- 解析完成后先显示，缓存改为后台原子写入。
- 解析时同步计算 XYZ 包围盒并保存到缓存头。
- 使用大块读取、换行对齐分块和 `std::from_chars` 加速 ASCII。
- 文件夹扫描后以单个低优先级任务依次预热其他 PLY 缓存。
- 可先显示真实点预览，但完整点云发布前禁用处理功能。

任何预览和降采样都不得在最终结果中使用质心或虚拟点替代真实测量点。

## 点云清理

默认参数：

| 参数 | 默认值 |
|---|---:|
| 体素尺寸 | 0.25 mm |
| 统计邻域 K | 45 |
| 标准差倍数 | 1.30 |

体素处理在每个体素中保留一个真实输入点，不生成质心。统计离群值算法使用空间
网格建立邻域，根据 K 近邻平均距离计算全局均值和标准差：

```text
threshold = globalMean + stddevMultiplier * globalStddev
```

当邻居不足时扩大搜索范围或降低实际 K。参数过严、有效邻域不足或结果为空时，
保留处理前缓存并提示用户，不得删除全部点。

## 三点平面提取

界面操作只保留：

```text
取点
放弃取点
撤销选择的点
确定平面
确定候选平面
取消确定平面
```

Esc 取消当前选择，Backspace 撤销上一个点。三个点必须由当前画布 GPU Picking
返回，空白位置不得选点。

初始平面：

```cpp
QVector3D a = p2 - p1;
QVector3D b = p3 - p1;
QVector3D normal = QVector3D::crossProduct(a, b);
normal.normalize();
float d = -QVector3D::dotProduct(normal, p1);
```

平面方程：

```text
normal.x*x + normal.y*y + normal.z*z + d = 0
```

法向量阈值根据当前点云包围盒计算。三点过近或共线时拒绝生成平面。

最终算法：

```text
三点初始平面
→ 初始距离阈值候选点
→ RANSAC 内点筛选
→ PCA/最小二乘精拟合
→ 最终距离分类
→ UV/XY 连通域保留目标区域
```

三点用于指定目标区域，RANSAC 用于抵抗离群点，PCA 最小特征值对应的特征向量
作为精拟合法向量。结果输出平面方程、RMS 距离、候选点、平面点和连通域统计。

## GPU Picking 精度

Picking 使用独立颜色 ID + 深度 FBO：

```text
当前模型矩阵/旋转/缩放/平移
→ 当前 Viewport 和 devicePixelRatio
→ 与正常渲染一致的点大小
→ 深度测试选择最前点
→ glReadPixels 读取 ID
```

中心像素为空时可以读取 3×3 GPU 像素区域，只接受有效 GPU ID。禁止 CPU 最近点
搜索和近似反投影。普通模式保留左键旋转、右键平移和滚轮缩放。


## 边缘和二维图片

默认算法：

```text
确认平面点
→ 平面 UV 投影
→ 自适应尺寸二值 Mask
→ 闭运算
→ 开运算
→ 8 邻域边界检测
→ Marching Squares 连续轮廓
→ 黄色边缘点和边界线
```

默认参数：

| 参数 | 默认值 |
|---|---:|
| 栅格尺寸 | 0.2 mm |
| 闭运算半径 | 1 |
| 开运算半径 | 1 |
| 最大栅格数 | 4,000,000 |
| 图片超采样倍数 | 3 |

如果 0.2 mm 会超过最大栅格数，自动增大实际栅格尺寸并显示最终值，不裁剪工件。
二维图片根据平面物理尺寸自适应分辨率，背景必须为纯黑 RGB `(0,0,0)`，工件
像素单独写入，便于 OpenCV 后续处理。

黄色边缘 Mask 可以直接在三维点云画布中选择，选择结果保持当前画布真实点索引。

## 显示规则

- 普通点：当前高度色、灰阶或法向量颜色模式。
- 提取平面点：灰色。
- P1/P2/P3：更亮灰色并放大。
- 边缘点和轮廓：黄色。

点云始终使用 `GL_POINTS` 和 VBO，不使用 QPainter 全量绘制。OpenGL 资源必须在
有效上下文中创建和释放。

## 输出文件

程序当前可输出：

- 平面二维 PNG（`Format_Grayscale8`，前景 `255`、背景 `0`）；
- 与 PNG 成套的 binary little-endian 平面 PLY 和 JSON 元数据；
- 用户本地应用数据 `cache/` 中的 `.pcvbin` 加载缓存；
- 用户本地应用数据 `logs/startup.log` 启动诊断日志。

PNG、PLY、JSON 由共享 `pcv_output` 契约 writer 写出，JSON 最后通过 `QSaveFile`
原子提交；任一文件写入失败，整体输出失败。

二维图片背景为纯黑色，尺寸根据平面物理范围和实际栅格自动生成。缓存和日志属于
运行文件，不作为源码交付内容。

## 当前验证状态

- Qt 6.8.3 MSVC x64 Debug 编译和链接成功。
- `windeployqt` 部署成功。
- `mainwindow.ui` 已通过 `uic`。
- 边缘默认值已确认为 `0.2 mm / 闭 1 / 开 1`。
- 最近一次未重新运行完整算法测试和人工 GUI 测试。

仍需人工验证：

- ASCII/Binary PLY 首次和缓存加载；
- 文件夹多 PLY 列表；
- 旋转、平移、缩放和高 DPI Picking；
- 重叠点最前深度选择和空白区域不选点；
- 统计滤波不会删除全部点；
- 三点共线/过近拒绝和拟合误差；
- 边缘轮廓和纯黑背景图片。

## 参数建议

- 统计离群值先使用 `K=45`、`1.30`；速度不足时先做代码计时，不直接降低精度。
- 边缘处理先使用 `0.2 mm / 闭 1 / 开 1`，小缺口增大闭运算，细小孤立噪声
  增大开运算。
- 平面初始容差和最终容差应根据传感器深度噪声及点间距调整。
- 超大平面应关注实际栅格尺寸；达到四百万格上限后程序会自动扩大栅格。

## 已知限制

- Designer 中 `lbl_subtitle`、`lbl_source_title`、`wgt_source_panel` 等 objectName
  重复，`uic` 会警告并自动重命名生成成员。
- 千万级点云需要完整 CPU 内存和显存，资源不足时加载或 VBO 上传会失败。
- ASCII PLY 首次加载仍受浮点解析、缓存写入和 VBO 上传影响；读取器已支持内存映射和自适应并行解析。
- 平坦 2.5D 表面可能缺少 XY 方向约束，平面目标应尽量包含孔、台阶或边缘。
- `registration_diagnostic_runner.cpp` 是未构建历史源文件，不属于主程序功能。
- `D:/workpiece/ply` 下旧 `.pcvbin` 因外部权限 503 暂未迁移。

## 交付和备份

源码交付保留 C++、UI、CMake、测试和本文档，排除 `build*`、`.git`、
`.qtcreator`、构建目录、DLL/EXE/PDB/OBJ、`.pcvbin`、诊断图片和生产点云。

有效源码备份：

```text
D:/workpiece/pointcloudview/backups/pointcloudview_20260818_obstacle_warning_clean
```

首次失败目录 `backups/pointcloudview_20260818_obstacle_warning` 包含生成文件，
不作为有效备份。Git 操作当前暂停，不自动 add、commit 或 push。

## 后续需求文档格式

以后新增或更新需求档案统一采用本文结构：

1. 标题、主程序、界面和核心算法入口；
2. 已支持的数据和功能；
3. 启动和构建命令；
4. 按实际执行顺序描述默认算法；
5. 分功能说明算法原理、默认参数和异常处理；
6. 罗列输出文件；
7. 记录真实编译、测试和现场验证结果；
8. 给出参数建议；
9. 列出已知限制；
10. 最后记录交付、备份和本次变更。

文档只写已经实现或明确待实现的内容。不得把参考项目的路径、参数、测试结果或
算法状态复制成当前项目事实。验证数据必须注明来源和实际执行结果。

## 本次修改记录

### 2026-08-18：按参考 README 结构重排

- 参考 `D:/Share/Point_Cloud_Stitching/README_zh.md` 的信息组织方式；
- 重排为“功能、启动、默认流程、算法、输出、验证、参数、限制”的现场审阅结构；
- 保留当前主程序真实实现，不引入参考拼接项目的路径、算法或测试数据；
- 固定后续需求档案的统一输出模板；
- 未修改代码和 `pointcloudstitch/README.md`，未重新编译，未执行 Git 操作。

### 2026-08-18：ASCII PLY 直接后台加载优化

- 画布的两个 PLY 加载入口改为 `QtConcurrent::run(loadPlyResult, ...)`，直接读取原始文件，不创建或读取 `.pcvbin` 中间缓存；
- 保持 ASCII PLY 支持，解析前使用 Header 声明的顶点数量执行 `QVector::reserve`；
- ASCII 顶点读取改为复用 64 KiB 字符缓冲和 `QByteArray::fromRawData`，减少每个顶点的临时 `QByteArray` 堆分配；
- 保留现有缓存接口供独立批处理代码兼容使用，但主画布加载路径不再调用缓存接口；
- `pointcloudstitch` 的 ASCII 读取器同步采用预分配和复用行缓冲；超长顶点行会明确报错，不会静默截断；
- 验证：`pointcloudview` Debug 构建通过，`pointcloudstitch` 构建通过，现有点云处理器测试和障碍检测测试均通过；
- 未修改二进制 PLY 输出格式、去噪、平面、边缘、OpenGL 渲染和配准算法，未执行 Git 操作。

### 2026-08-18：后台 ASCII PLY 读取进度

- 新增 `LoadProgressCallback`，ASCII 顶点读取每 10000 点向 GUI 报告一次已读点数；
- 单文件打开和文件夹首文件打开均通过 `QtConcurrent` 调用带进度的加载入口；
- 工作线程只负责文件读取和解析，进度更新使用队列调用回到主线程；OpenGL/VBO 仍只在画布线程执行；
- 使用 `QPointer<MainWindow>` 防止窗口关闭后后台进度回调访问悬空窗口；
- 进度条从不定范围改为 `0～1000`，显示“已读取点数/总点数”，读取完成后仍统一发布完整点云，保持 Picking 索引稳定；
- 验证：`pointcloudview` Debug 构建通过，点云处理器测试和障碍检测测试通过；
- 当前阶段未实现分块 VBO 追加显示，避免在加载过程中重置平面、边缘、障碍和 Picking 状态。

### 2026-08-18：ASCII 数值解析优化

- ASCII 顶点坐标解析由 `strtod(double)` 改为 `strtof(float)`，直接生成渲染和算法使用的单精度值；
- 保持科学计数法、负数、任意属性顺序和 `x/y/z` 必选规则不变；
- 不改变点数量、点顺序、法向量字段或后续处理接口；
- `pointcloudstitch` 的 ASCII 解析器同步采用单精度转换；
- 验证：`pointcloudview` 和 `pointcloudstitch` 构建通过，点云处理器测试和障碍检测测试通过；
- 本阶段不修改 VBO、OpenGL、二进制输出和缓存策略。

### 2026-08-18：画布点坐标查询

- 普通视图和取点模式均支持左键单击，通过现有 GPU Picking 获取当前画布真实点索引；
- 左键拖动仍执行旋转，右键拖动仍执行平移，空白位置不生成坐标；
- 命中点后在画布右上角显示点索引以及 `X/Y/Z`，单位为毫米并保留三位小数；
- PLY 含有效 `nx/ny/nz` 时，根据法向量显示 `A/B/C`：`A=atan2(ny,nz)`、`B=atan2(-nx,hypot(ny,nz))`、`C=0`；
- 单个法向量不能唯一确定绕法向量的旋转，因此 `C=0` 是显示约定；没有有效法向量时 `A/B/C` 显示 `--`，不伪造机器人姿态；
- 新点云发布时清除旧查询点，避免显示旧点索引；平面三点选择和边缘选择继续复用同一 GPU Picking 结果；
- 验证：`pointcloudview` Debug 构建通过，点云处理器测试和障碍检测测试通过；GUI 实际点选位置需在本机加载点云后人工确认。

### 2026-08-19：三点建立工件坐标系（第二阶段）

- 在现有平面提取流程中，用户确认候选平面后，按当前画布缓存中的 P1/P2/P3 建立正式工件坐标系；所有输入点均视为机器人基坐标系坐标。
- 默认工件原点为 P1；工件 X 轴取 P1→P2 在三点平面内的单位方向，工件 Z 轴取 `(P2-P1) × (P3-P1)` 并默认统一到机器人正 Z 半空间，工件 Y 轴取 `Z × X`，保证右手坐标系。
- 三点数量错误、索引越界、点间过近、近似共线、轴方向退化或矩阵不可逆时拒绝建立坐标系并显示明确原因。
- 计算并保留 `T_base_workpiece`（工件坐标到机器人基坐标）和 `T_workpiece_base`（机器人基坐标到工件坐标）；姿态角采用 `Rz(C) × Ry(B) × Rx(A)`，单位为度。
- 平面确认输出中显示工件原点机器人基坐标、A/B/C、三条单位轴、正交误差和完整 `T_base_workpiece` 矩阵。
- 画布使用 QPainter 投影显示工件原点及红色 X、绿色 Y、蓝色 Z 三条坐标轴；旋转、缩放、平移后坐标轴使用与点云一致的当前视图矩阵重新投影。
- 新点云发布、重新取点、放弃取点、撤销或取消候选平面时同步清空旧工件坐标系，禁止旧矩阵继续作用于新画布缓存。
- 本阶段暂不包含用户单独选择工件中心、工件坐标 PNG/JSON 输出和机器人通讯；这些功能基于本阶段矩阵在后续阶段实现。
- 验证：Qt 6.8.3 / MSVC x64 Debug 主程序构建通过；测试覆盖右手坐标轴、正 Z 约定、机器人基坐标到工件坐标逆变换、零姿态和共线拒绝，CTest `1/1 passed`。

### 2026-08-19：平面中心原点、独立 X/Y 轴与矩形平面图像

- 三点只用于拟合和提取目标平面，不再直接作为工件坐标系原点和轴定义。
- 平面确认后，在完整平面点集合上计算 XY Z 包围盒中心；该中心作为后续工件坐标原点和 2D 图像中心，避免孔洞或点密度不均导致质心偏移。
- 新增“选择 X/Y 轴点”：用户在画布中依次选择 X 轴点和 Y 轴点，两个点不能与三点种子重复，也不能与平面中心重合。两条方向投影到已拟合平面并正交化，生成右手工件坐标系。
- 坐标系矩阵、姿态角和画布 XYZ 轴显示均使用新的平面中心原点；清除轴点或重新加载/取点会同步清除旧坐标系。
- 平面 2D 图像提取支持用户设置矩形宽度和高度（毫米）。图像物理坐标以平面中心为 `(0,0)`，矩形范围为 `[-width/2,width/2] × [-height/2,height/2]`，超出矩形或非工件区域保持纯黑 RGB(0,0,0)。
- 图像 X/Y 方向使用用户确定的工件 X/Y 轴，仍保持原有栅格尺寸和放大倍数；可直接作为 OpenCV 后续输入。
- 画布俯仰旋转取消 `±89°` 限制，鼠标拖动可连续旋转完整 360°，不改变点选、平移和缩放操作。
- 验证：Qt 6.8.3 / MSVC x64 Debug 构建成功，CTest `1/1 passed`；Designer 控件已通过 uic。Qt Designer 仍提示历史重复 objectName 自动重命名，不影响本次新增控件。

### 2026-08-19：工件 Z 轴绑定拟合平面法向

- 工件 Z 轴不再由两个用户轴点直接叉乘估计，而是直接采用最终 RANSAC/PCA 拟合平面的单位法向量，因此默认严格垂直于当前提取平面。
- 用户 X 轴点先投影到拟合平面内并归一化；Y 轴由 `Z × X` 得到，保证 X/Y 均位于平面内且坐标系保持正交右手系。
- 用户 Y 轴点只用于判断 Y 方向及平面法向的正反；X/Y 轴点近似共线、靠近中心或方向退化时拒绝建立坐标系。
- 2D 图像继续使用该工件 X/Y 平面内轴，因此图像法向与工件 Z 轴一致。
- 验证：Qt 6.8.3 / MSVC x64 Debug 构建成功。

### 2026-08-19：PNG 与同名 JSON 成对输出

- “保存 2D 图片”现在固定输出 PNG，并在同一目录使用相同基名自动生成 JSON，例如 `result_001.png` 与 `result_001.json`。
- JSON 按 `requirement.md` 的 1.0.0 结构输出：任务号、PLY 文件、单位、按点击顺序保存的三点机器人基坐标、平面中心原点、工件 `X/Y/Z/A/B/C`、工件到机器人基坐标矩阵、图像宽高/通道/位深、物理边界、像素原点和输出状态。
- 当前平面图像输出为 8 位单通道二值 PNG：背景为 0，工件有效栅格为 255；图像坐标原点位于左上角，U 对应工件 X 正方向，V 对应工件 Y 负方向。
- JSON 的 `hand_eye_calibration.available` 会真实反映当前界面是否执行了手眼转换。当前画布若已是机器人基坐标缓存，则记录 `available=false` 和说明，不伪造标定矩阵。
- PNG 写入成功后才写 JSON；PNG 或 JSON 任一写入失败都会弹出明确警告，不报告为成对成功。
- 物理图像范围来自用户设置的矩形宽高，中心为工件坐标 `(0,0)`；输出图像和 JSON 的宽高、边界和像素原点保持一致。
- 验证：Qt 6.8.3 / MSVC x64 Debug 构建通过，CTest `1/1 passed`。

### 2026-08-19：修复加载期间画布不显示与 Debug 堆断言

- 根因：最近的进度回调让 ASCII PLY 后台任务捕获 `QPointer<MainWindow>` 并持续向窗口投递队列消息；窗口关闭或 watcher 析构时，超大 `LoadResult` 又通过 `result()` 复制并保留在 watcher 内部，关闭阶段容易触发 `_CrtIsValidHeapPointer`，加载结果也会在画布发布前被丢弃。
- 修复：后台加载任务现在只捕获不可变文件路径，不再触碰 `MainWindow` 或向 GUI 投递进度回调；进度条恢复为不定范围，完整读取完成后一次性发布缓存。
- 修复：`loadFinished()` 使用 `QFuture::takeResult()` 移动接管结果，并将点数据移动到主缓存，避免 watcher、结果局部变量和窗口同时持有大块结果；关闭和析构前先断开全部 watcher 完成信号，再等待任务结束。
- 修复：事件循环退出后恢复 Qt 默认日志处理器，避免日志文件处理器参与 QApplication/OpenGL teardown。
- 保持：仍使用后台线程读取 ASCII PLY、Header 顶点数 `reserve` 和复用行缓冲；不引入 CUDA、PCL、VTK，不改变点顺序和 GPU Picking 索引。
- 验证：Qt 6.8.3 / MSVC x64 Debug 构建通过；CTest `1/1 passed`；`POINTCLOUDVIEW_SELFTEST_CLOSE=1` 启动/关闭烟测正常退出；OpenGL 初始化路径保持 RTX 4060 原生桌面驱动。

### 2026-08-19：第三阶段——工件平面与点云映射

- 正式处理链路固定为：机器人基坐标画布缓存 → 三点初始平面 → 候选点提取 → RANSAC/PCA 精拟合 → 距离阈值分类 → 连通域过滤 → 工件坐标映射。
- 平面和图像处理只读取当前 `m_points` 画布缓存，不重新读取原始 PLY，也不重复套用手眼矩阵。进入本阶段的点必须已经处于机器人基坐标系。
- `extractPlaneImage()` 对最终平面索引逐点执行显式映射：`P_workpiece = R_base_workpiece^T * (P_base - O_base)`，其中 X/Y 为用户确认的工件轴，Z 为拟合平面法向方向，平面距离作为局部 Z 坐标。
- 图像导出阶段再次以 `planeDistanceTolerance`（默认 0.4 mm）检查点到最终拟合平面的距离；无效点、平面外点和矩形外点均不会写入 valid mask，因此不会进入 PNG 前景。
- `PlaneImageResult` 记录输入索引数、实际映射点数、平面外拒绝数、无效点数、矩形裁剪数、工件坐标最小/最大范围和实际距离阈值。界面摘要与同名 JSON 均输出这些统计。
- JSON 新增 `mapping` 对象，明确源坐标系、目标坐标系、转换公式、映射统计、平面容差和工件坐标范围；`workpiece_coordinate` 继续输出原点、X/Y/Z 轴、A/B/C 和 4×4 矩阵。
- 坐标系版本发生变化（重新取点、撤销、放弃、清除轴点或重新确认 X/Y）时，旧平面图像结果立即失效；后台图像任务返回时同时校验画布版本和坐标系版本，禁止旧坐标系结果覆盖新画布。
- 验收测试覆盖远离机器人基坐标原点的平面、局部坐标范围、平面外点拒绝和工件图像生成；Qt 6.8.3 / MSVC x64 Debug 构建成功，CTest `1/1 passed`。

### 2026-08-19：Todo 阶段一——自动平面边界与固定像素比例

- 平面 PNG 不再使用用户手动输入的宽度和高度；仅使用最终连通域平面点在工件坐标系中的 X/Y 范围计算边界。
- 自动边界在 X/Y 四个方向各增加 50 mm，扩展区域填充纯黑。最终宽度和高度均向上取整到 10 mm 的整数倍，并保持工件坐标原点位于图像中心。
- 输出像素比例固定为 `1 px = 0.05 mm`（20 px/mm），自动边界模式下不再使用旧的预览放大倍数和手动矩形尺寸。
- 自动模式默认最大像素数为 100,000,000。超过限制时后台任务返回明确错误，避免大尺寸 QImage 分配导致程序崩溃；实际部署可根据内存调整该上限或后续实现分块写出。
- `PlaneImageResult` 新增自动边界、原始平面尺寸、边距、像素尺寸和映射统计字段；界面仍保留历史 Designer 控件但已禁用手动宽高输入。
- JSON 的 `image` 增加 `pixel_size_mm`、`pixels_per_mm`、`automatic_bounds`、`margin_mm`、`round_increment_mm`；增加 `workpiece_center_in_workpiece`、`workpiece_center_in_robot_base` 和 `mapping` 节点，声明可逆坐标关系和拒绝统计。
- 阶段验收：固定像素比例、自动边界、10 mm 递进和远离机器人原点的映射测试通过；Debug 构建成功，CTest `1/1 passed`。

### 2026-08-19：Todo 阶段二——提取平面机器人基坐标 PLY 输出

- 保存 PNG 时同步输出同名基础文件名的 `_plane_robot_base.ply`，内容仅包含最终连通域过滤后的平面点，不包含平面外点、障碍点或原始缓存中的其他点。
- PLY 使用 binary little-endian 格式，保存 `x/y/z`、可用 `nx/ny/nz` 和 `source_index`。点坐标保持机器人基坐标，避免将工件局部坐标误当作机器人坐标。
- PLY header 写入 `source_frame`、`target_frame`、来源 PLY、平面方程、工件原点机器人基坐标和工件 A/B/C。完整逆变换矩阵仍由同名 JSON 保存。
- PNG、平面 PLY 和 JSON 任何一个写入失败都不会报告为完整输出；JSON `output` 增加 PLY 文件名、点数和写入状态。
- 阶段验收：Debug 构建成功，CTest `1/1 passed`；导出接口拒绝空索引、越界索引和无效点。

### 2026-08-19：Todo 阶段三——第二组三点平面一致性校验

- 在第一平面候选已确定并建立工件坐标系后，平面提取页新增“第二组三点校验”和取消按钮。
- 第二组三点只用于验证用户选取区域是否仍属于第一确认平面，不替换第一平面模型，也不重新读取原始 PLY。
- 校验统一比较法向夹角和三点到第一拟合平面的最大距离；默认法向夹角阈值为 `3°`，距离阈值为 `0.4 mm`，法向正负方向按绝对点积处理。
- 第二组三点共线、跨台阶、落在障碍物或距离/角度超限时弹出明确警告，并在平面输出区域记录数值；通过时显示同一平面确认结果。
- 第二组三点的点选状态在重新加载、重新取点、取消平面、撤销和发布新画布缓存时清空，不影响第一组三点、平面索引和工件坐标矩阵。
- 阶段验收：Qt Designer UIC、Qt 6.8.3 / MSVC x64 Debug 构建和 CTest `1/1 passed` 均通过。

### 2026-08-19：ASCII PLY 内存映射读取

- ASCII PLY 头解析完成后，顶点数据区优先使用 `QFile::map` 映射到内存，
  直接扫描换行和数值，减少逐行系统调用及临时缓冲分配。
- 映射路径继续使用自定义浮点解析器、预分配 QVector 和原始点顺序；映射失败
  自动回退到原有分块缓冲读取，不改变格式兼容性。

### 2026-08-20：分块并行解析阶段一——读取基线计时

- 当前阶段不改变 ASCII/Binary PLY 的解析算法，仅在成功读取结果中记录 Header、
  顶点解析和读取器总耗时（毫秒）。
- 主程序日志同时记录读取器完成和画布缓存发布边界，用于区分文件解析耗时与后续
  VBO 上传/首次绘制耗时；不弹出计时窗口，不改变加载状态和点云显示流程。
- 该基线是后续分块并行解析的对照组，必须使用相同文件、相同点数和相同属性模式比较。

### 2026-08-20：分块并行解析阶段二——ASCII 分块边界扫描

- ASCII 映射数据区按四个预备 Chunk 划分；每个理论字节边界都会向后校正到完整换行，
  确保一行顶点不会被两个 Chunk 重复或截断。
- 分块扫描统计每个 Chunk 的首点索引和点数，并验证所有 Chunk 点数总和等于 Header
  声明点数；本阶段仍按顺序串行解析，暂不启用并行浮点转换。
- 日志新增 `boundary_scan_ms`，用于衡量边界扫描成本；该阶段失败时不会发布部分点云，
  画布仍保持旧缓存。

### 2026-08-20：分块并行解析阶段三——并行解析

- 内存映射成功的 ASCII PLY 使用经过边界校验的 Chunk 并行解析；各 worker 分别写入
  预分配点数组的固定、不重叠区间，不使用 `append()`，保持原文件点顺序和索引。
- 每个线程独立统计解析点数、XYZ 最小/最大值和错误；两线程全部结束后统一合并，任何
  Chunk 失败都不会发布部分点云。
- 分块边界扫描只计数 Header 声明的 vertex 行，后续 face 或其他 ASCII 元素不再被误判
  为顶点。内存映射失败时继续使用原串行缓冲读取路径。
- 工作线程产生的具体解析或取消错误必须保留到最终结果，不得被末尾通用的“顶点数据
  不完整”错误覆盖，以便准确定位损坏行或取消位置。
- `ply_reader_tests.exe <PLY 路径>` 先执行内建 ASCII/Binary 回归用例，再输出指定生产
  样本的编码、点数、XYZ 边界和 Header/边界扫描/解析/总耗时，作为后续阶段的固定对照。
- 真实样本 `4_Point_Cloud_A04_robot_base.ply` 实测为 binary little-endian、4,741,478 点；
  XYZ 边界分别为 `596.544..782.812`、`-74.5142..227.849`、`934.403..1019.61 mm`，
  本机 Debug 读取器总耗时约 8,026 ms。该样本验证共用读取契约，ASCII 双线程性能仍需以
  同规模 ASCII 生产文件作后续对照。

### 2026-08-20：分块并行解析阶段四——自适应线程数

- 内存映射成功后，根据 ASCII 数据规模自动选择并行度：小文件单线程、中型文件最多
  双线程、大型文件最多四线程；同时受 CPU 报告的硬件并发数约束，避免低核心机器过载。
- `PlyReadOptions::asciiWorkerCount` 默认为 `0`（自动），测试时可显式指定 `1..8`；生产
  GUI 不提供线程数控件，避免用户参数破坏稳定性。映射失败的缓冲回退仍固定单线程。
- `PlyReadResult::asciiWorkerCount` 记录本次实际线程数；测试命令扩展为
  `ply_reader_tests.exe <PLY 路径> [线程数]`，用于同一 ASCII 文件的 1/2/4 线程对照。
- 阶段三真实 ASCII 基线采用 `Point_Cloud_A04.ply`：约 204 MB、6,300,000 点，
  首次记录边界扫描约 174 ms、解析约 1,015 ms、读取器总耗时约 1,016 ms。
- 阶段四同文件顺序对照结果：单线程约 1,597 ms、双线程约 953 ms、四线程约 653 ms、
  自动模式约 638 ms；自动模式选择四线程，较双线程缩短约 33%，较单线程缩短约 60%。
  四次读取均为 6,300,000 点，XYZ 边界完全一致，说明并行度变化未改变数据内容或点序契约。

### 2026-08-20：PLY 读取阶段五——紧凑 Binary XYZ 专用路径

- binary little/big-endian PLY 的 vertex 属性严格为连续 `float x/y/z` 时，直接映射顶点区，
  按点索引划分固定区间并行解码到预分配 `Point3D` 数组；线程只写各自区间并独立统计边界。
- 二进制快速路径在读取器内部自动选择 1/2/4 线程，不把线程数加入跨模块结果结构。
  含法向量、颜色、强度、属性重排或其他标量类型的文件继续使用通用解析器；映射失败同样自动回退，不降低兼容性。
- 指定生产样本 `4_Point_Cloud_A04_robot_base.ply` 为 binary little-endian、56.9 MB、
  4,741,478 点。原通用 Debug 路径约 8,093 ms；专用路径同轮实测单线程 501 ms、双线程
  295 ms、四线程 214 ms、自动模式 242 ms，自动模式选择四线程，较原路径缩短约 97%。
- 四组结果的点数和 XYZ 边界均一致：X `596.544..782.812`、Y `-74.5142..227.849`、
  Z `934.403..1019.61 mm`。该优化不改变点顺序、坐标系和画布缓存发布流程。
- 主画布未提供逐点进度回调时，不再为每个点执行共享原子进度累加；取消轮询、点数校验
  和最终发布规则不变。调用方确实提供进度回调时仍按原频率报告。
- 去除无效进度原子累加后，同一样本自动四线程复测约 166 ms；相对原通用路径约
  8,093 ms 缩短约 98%。实际 GUI 总耗时还包括结果交接、画布缓存发布和 OpenGL 上传。

### 2026-08-20：修复 PLY 读取器 Debug 栈保护区损坏

- 原因：阶段五把 `binaryWorkerCount` 追加到公共 `PlyReadOptions` 和 `PlyReadResult`。
  Qt Creator 增量构建可能只重建读取库而保留旧的应用目标文件；新旧结构体大小不一致时，
  MSVC 会在返回 `PlyReadResult parsed` 时报告 `Run-Time Check Failure #2`，表现为
  “Stack around the variable 'parsed' was corrupted”。
- 修复：二进制线程数恢复为 `ply_reader.cpp` 内部实现，不再改变公共结果结构体 ABI；
  诊断测试改为验证自动路径。完整重建后，紧凑 Binary XYZ 快速路径和通用 PLY 回退路径均保持不变。
- 构建修复：`pointcloudview` 和 `pointcloudstitch` 的处理器源文件通过 CMake
  `OBJECT_DEPENDS` 显式依赖 `ply_reader.h`。即使本机 MSVC 中文 `/showIncludes` 未生成有效
  `.obj.d` 文件，读取接口变化也会强制重新编译调用方，禁止新读取库与旧对象文件混合链接。
- 验证：Qt 6.8.3 / MSVC x64 Debug 主程序构建通过，`ply_reader_tests` 通过，目标样本点数和
  XYZ 边界保持 `4,741,478` 点及原值。

### 2026-08-20：边缘分割后的平面 2D 导出约束

- 边缘分割完成后，2D 导出仍以确认平面的有效点和工件坐标轴为输入；边缘 Mask
  不改变平面点的坐标映射、自动边界或机器人基坐标信息。
- 导出矩形始终由有效平面点的 X/Y 边界自动计算，四边各增加 50 mm，再向上取整到
  10 mm 的整数倍；旧的手动宽度/高度控件不参与导出。
- 固定像素比例为 `1 px = 0.05 mm`。边界外和非工件区域写入纯黑（灰度 0），平面
  前景写入 255，供 OpenCV 后处理。
- PNG 同名 JSON 记录物理/像素宽高、像素比例、边距、取整步长、工件中心 `X/Y/Z/A/B/C`、
  工件坐标变换矩阵和映射统计，支持由 PNG/JSON 反推平面点云坐标。
- 确定平面和 X/Y 轴后，界面输出框及状态栏显示工件坐标原点在机器人基坐标系中的
  `X/Y/Z/A/B/C`；该值与 PNG 同名 JSON 的 `workpiece_coordinate.pose_in_robot_base` 一致。

### 2026-08-20：单通道二值图像值约束

- 平面提取和边缘分割输出统一为 `QImage::Format_Grayscale8` 单通道图像。
- 工件/平面前景像素固定为灰度 `255`，非工件区域、背景和孔洞固定为 `0` 纯黑；
  不再输出黄色边缘或 RGB 灰阶，保证 OpenCV 读取时通道和阈值稳定。
- 边缘分割完成后，其生成的 Mask 会同步成为当前可保存的 2D 图像，沿用 PNG/JSON
  输出接口及上述像素值约束。
