#include <pcv/render/pointcloud_canvas.h>

// The widget implementation is currently kept inline in the public header
// for source compatibility with existing Qt forms.  This translation unit
// gives the module a stable implementation boundary; method bodies can move
// here incrementally without changing the public render contract.
